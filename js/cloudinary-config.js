// Fill in your Cloudinary credentials here.
// Sign up free (no card) at cloudinary.com, then find these in your dashboard.
const cloudinaryConfig = {
    cloudName: "dprvyrv7v",      // e.g. "dxyz123abc"
    uploadPreset: "guillotine"    // must be set to Unsigned in Cloudinary settings
};
